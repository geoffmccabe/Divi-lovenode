[package]
name = "lovenode-core"
description = "Divi proof-of-stake win-check. Pure consensus math: no I/O, no keys, no chain."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
sha2 = "0.10"
